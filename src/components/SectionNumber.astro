---
interface Props {
  value: string;
}

const { value } = Astro.props;
---

<span class="inline-flex h-12 w-12 items-center justify-center rounded-full border border-[var(--color-border)] text-sm font-medium text-[var(--color-text-soft)]">
  {value}
</span>
